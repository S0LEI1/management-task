import { ListPermission } from '../events/types/permissions';
import { UserType } from '../events/types/user_type';
export declare class Check {
    static isManager(type: string, permissions: string[], requirePermission: ListPermission[]): boolean;
    static checkTypeAndPermission(type: string, checkType: UserType, permissions: string[], requirePermission: ListPermission[]): boolean;
    static checkImage: (file: Express.Multer.File) => void;
    static checkExcel(file: Express.Multer.File): void;
}
