import { Subjects } from '../subjects';
import { UserStatus } from '../types/user_status';
export interface UserShiftDeletedEvent {
    subject: Subjects.UserShiftDeleted;
    data: {
        id: string;
        status: UserStatus;
        version: number;
    };
}
