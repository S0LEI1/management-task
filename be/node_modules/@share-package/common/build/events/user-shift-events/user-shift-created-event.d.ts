import { Subjects } from '../subjects';
import { UserShiftStatus } from '../types/user-shift-status';
export interface UserShiftCreatedEvent {
    subject: Subjects.UserShiftCreated;
    data: {
        id: string;
        userId: string;
        shiftId: string;
        status: UserShiftStatus;
        date: Date;
    };
}
