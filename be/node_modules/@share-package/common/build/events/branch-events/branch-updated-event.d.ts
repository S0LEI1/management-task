import { Subjects } from '../subjects';
export interface BranchUpdatedEvent {
    subject: Subjects.BranchUpdated;
    data: {
        id: string;
        name: string;
        phoneNumber: string;
        email: string;
        address: string;
        version: number;
    };
}
