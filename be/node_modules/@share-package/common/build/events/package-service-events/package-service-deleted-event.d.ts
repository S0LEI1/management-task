import { Subjects } from '../subjects';
export interface PackageServiceDeletedEvent {
    subject: Subjects.PackageServiceDeleted;
    data: {
        id: string;
        version: number;
        isDeleted: boolean;
    };
}
