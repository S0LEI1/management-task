import { Subjects } from '../subjects';
export interface PackageServiceCreatedEvent {
    subject: Subjects.PackageServiceCreated;
    data: {
        id: string;
        serviceId: string;
        packageId: string;
        quantity: number;
    };
}
