import { Subjects } from '../subjects';
export interface PackageServiceUpdatedEvent {
    subject: Subjects.PackageServiceUpdated;
    data: {
        id: string;
        serviceId: string;
        packageId: string;
        quantity: number;
        version: number;
    };
}
