import { Subjects } from '../subjects';
export interface RolePermissionCreatedEvent {
    subject: Subjects.RolePermissionCreated;
    data: {
        id: string;
        permissionId: string;
        roleId: string;
        version: number;
    };
}
