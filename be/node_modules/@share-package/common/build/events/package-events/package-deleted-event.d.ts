import { Subjects } from '../subjects';
export interface PackageDeletedEvent {
    subject: Subjects.PackageDeleted;
    data: {
        id: string;
        isDeleted: boolean;
        version: number;
    };
}
