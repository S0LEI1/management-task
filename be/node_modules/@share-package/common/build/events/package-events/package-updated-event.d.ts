import { Subjects } from '../subjects';
export interface PackageUpdatedEvent {
    subject: Subjects.PackageUpdated;
    data: {
        id: string;
        name: string;
        description: string;
        imageUrl: string;
        salePrice: number;
        featured: boolean;
        discount: number;
        count: number;
        expire: number;
        code: string;
        version: number;
    };
}
