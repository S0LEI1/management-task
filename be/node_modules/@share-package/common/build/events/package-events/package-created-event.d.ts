import { Subjects } from '../subjects';
export interface PackageCreatedEvent {
    subject: Subjects.PackageCreated;
    data: {
        id: string;
        name: string;
        description: string;
        imageUrl: string;
        salePrice: number;
        featured?: boolean;
        discount?: number;
        count: number;
        expire: number;
        code: string;
    };
}
