import { Subjects } from '../subjects';
interface UsageLog {
    date: Date;
    status: boolean;
}
interface ServiceEmbedded {
    service: string;
    status: boolean;
    quantity: number;
    usageLogs?: UsageLog[];
}
export interface OrderPackageCreatedEvent {
    subject: Subjects.OrderPackageCreated;
    data: {
        id: string;
        order: string;
        package: string;
        serviceEmbedded: ServiceEmbedded[];
        quantity: number;
        totalPrice: number;
        execEmployee?: string;
    };
}
export {};
