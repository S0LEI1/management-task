import { Subjects } from '../subjects';
export interface SuplierCreatedEvent {
    subject: Subjects.SuplierCreated;
    data: {
        id: string;
        name: string;
        phoneNumber: string;
        email: string;
        address: string;
        description: string;
        code: string;
    };
}
