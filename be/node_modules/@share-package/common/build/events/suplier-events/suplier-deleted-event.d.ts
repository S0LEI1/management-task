import { Subjects } from '../subjects';
export interface SuplierDeletedEvent {
    subject: Subjects.SuplierDeleted;
    data: {
        id: string;
        version: number;
        isDeleted: boolean;
    };
}
