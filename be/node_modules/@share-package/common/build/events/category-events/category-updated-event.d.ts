import { Subjects } from '../subjects';
export interface CategoryUpdatedEvent {
    subject: Subjects.CategoryUpdated;
    data: {
        id: string;
        name: string;
        description: string;
        code: string;
        version: number;
    };
}
