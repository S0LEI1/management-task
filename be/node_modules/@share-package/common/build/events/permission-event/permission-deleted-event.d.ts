import { Subjects } from '../subjects';
export interface PermissionDeletedEvent {
    subject: Subjects.PermissionDeleted;
    data: {
        id: string;
        active: boolean;
        version: number;
    };
}
