import { Subjects } from '../subjects';
export interface ShiftUpdatedEvent {
    subject: Subjects.ShiftUpdated;
    data: {
        id: string;
        description: string;
        version: number;
    };
}
