import { Subjects } from '../subjects';
import { ShiftOptions } from '../types/shift-options';
export interface ShiftCreatedEvent {
    subject: Subjects.ShiftCreated;
    data: {
        id: string;
        shiftOptions: ShiftOptions;
        description: string;
    };
}
