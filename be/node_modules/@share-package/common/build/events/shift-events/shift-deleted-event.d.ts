import { Subjects } from '../subjects';
export interface ShiftDeletedEvent {
    subject: Subjects.ShiftDeleted;
    data: {
        id: string;
        version: number;
        isDeleted: boolean;
    };
}
