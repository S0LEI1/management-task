import { Subjects } from '../subjects';
export interface OrderServiceCreatedEvent {
    subject: Subjects.OrderServiceCreated;
    data: {
        id: string;
        order: string;
        service: string;
        quantity: number;
        totalPrice: number;
        execEmployee?: string;
    };
}
