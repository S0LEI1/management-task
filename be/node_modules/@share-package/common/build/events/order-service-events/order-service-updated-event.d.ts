import { Subjects } from '../subjects';
interface UsageLog {
    date: Date;
    status: boolean;
}
export interface OrderServiceUpdatedEvent {
    subject: Subjects.OrderServiceUpdated;
    data: {
        id: string;
        order: string;
        service: string;
        quantity: number;
        totalPrice: number;
        execEmployee: string;
        usageLogs: UsageLog[];
        version: number;
    };
}
export {};
