import { Subjects } from '../subjects';
export interface ProductUpdatedEvent {
    subject: Subjects.ProductUpdated;
    data: {
        id: string;
        name: string;
        imageUrl: string;
        description: string;
        suplierId: string;
        categoryId: string;
        expire: Date;
        salePrice: number;
        quantity: number;
        discount: number;
        featured: boolean;
        version: number;
        code: string;
    };
}
